# Nexus Draw — Environment Variables
# Copy this file to .env and adjust if needed.
# No AI API key required — the AI bots use a custom CNN model
# trained entirely in the browser with TensorFlow.js.

# Optional: Server port (most hosts set this automatically)
PORT=3001

# Optional: environment flag
NODE_ENV=production
